// The demo is a fully client-side interactive sandbox (shared in-memory store,
// timers, live updates). It has no server data and no SEO value, so skip SSR
// and prerendering — render it on the client only.
export const ssr = false;
export const prerender = false;
